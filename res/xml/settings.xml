<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >
        <EditTextPreference
        android:dialogTitle="Enter desired distance (miles)"
        android:inputType="numberDecimal"
        android:key="runningDistance"
        android:title="Running Distance"
        android:summary="Find a route with a total distance around this distance" android:defaultValue="@string/default_distance"/>

        <EditTextPreference
        android:dialogTitle="Enter search radius (miles)"
        android:inputType="numberDecimal"
        android:key="searchRadius"
        android:title="Search Radius"
        android:summary="Find routes with a start point within this radius" android:defaultValue="@string/default_search_radius"/>
        
        
    

</PreferenceScreen>